{
  "name": "<%= extName.toLowerCase() %>",
  "version": "<%= extVersion %>",
  "exports": {
    ".": "./generators/app/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sfdx",
    "sf-extend",
    "extend",
    "extension",
    "yeoman-generator"
  ],
  "dependencies": {
    "sf-extension": "^<%= baseVersion.split('.')[0].replace('^','') %>.x"
  },
  "peerDependencies": {
    "<%= pkg.name %>": "^<%= pkg.version.split('.')[0] %>.x"
  }
}
